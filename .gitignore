# IDE / Editor
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Secrets / local env
.env
.env.*
!.env.example

# Logs & temp
*.log
logs/
tmp/
temp/

# Playwright MCP browser dumps / snapshots
.playwright-mcp/

# Repo-root runtime artifacts (subtitle extract, local video runs)
/outputs/

# Manual CLI stdout/stderr redirects (e.g. indextts2_generate.py > p1.out 2> p1.err)
p*.out
p*.err

# Local agent skill copies (install via cross-agent-install docs)
.claude/
.agents/
.cursor/

# Local skill distribution archives (pack manually; do not commit)
video-producer.zip

# IndexTTS2 intermediate beat WAV (removed after padding; may linger on Windows)
*.__raw__.wav

# video-producer: Chinese audit mirrors (not part of skill runtime)
video-producer/audit/

# video-producer: local/scratch test scripts (do not ship)
video-producer/scripts/test_*.py
video-producer/scripts/*_test.py

# sticker-kit / cozy-sticker-ui: local render dumps (frames, sheets, GIFs)
sticker-kit-output/
cozy-sticker-ui-output/
**/sticker-kit-output/
**/cozy-sticker-ui-output/

# sticker-kit: local smoke / compile / init scratch dirs
.tmp-sticker-kit-*/

# Skill-local runtime outputs (examples live under references/, not here)
ai-glitch-comedy-filmmaker/outputs/

# Local scratch: implementation planning notes (not part of skill runtime)
/planning/

# Local Video Producer project instances (init_project output; may contain
# node_modules, voice WAVs, research dumps, renders — keep out of the skill repo)
/projects/
