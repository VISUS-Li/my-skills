# Review Studio Plan

`review-studio/` is a separate visual editing web service. It is intentionally outside the core director workflow for this skill revision.

Use the studio only when the user explicitly asks to inspect, edit, or run the visual review UI. Normal video production should follow `SKILL.md` and the director references instead of loading this file.
